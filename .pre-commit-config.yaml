repos:
  - repo: local
    hooks:
      - id: pymarkdown
        name: pymarkdown
        entry: pymarkdown --config .pymarkdown.yaml scan
        language: python
        additional_dependencies: ["pymarkdownlnt==0.9.36"]
        types_or: [markdown]
        stages: [manual]
