watch_file environment.yml

if ! command -v micromamba >/dev/null 2>&1; then
  echo "micromamba not found; install it before using this repository."
  exit 1
fi

eval "$(micromamba shell hook --shell bash)"
micromamba activate phage_env
