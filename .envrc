watch_file environment.yml

if ! command -v conda >/dev/null 2>&1; then
  echo "conda not found; install Miniconda before using this repository."
  exit 1
fi

eval "$(conda shell.bash hook)"
conda activate phage_env
